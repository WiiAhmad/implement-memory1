diff --git a/TencentDB-Agent-Memory/src/core/store/sqlite.ts b/TencentDB-Agent-Memory/src/core/store/sqlite.ts
index f24c217..bde9ded 100644
--- a/TencentDB-Agent-Memory/src/core/store/sqlite.ts
+++ b/TencentDB-Agent-Memory/src/core/store/sqlite.ts
@@ -21,7 +21,6 @@
  */

 import { createRequire } from "node:module";
-import type { DatabaseSync, StatementSync } from "node:sqlite";
 import type { MemoryRecord } from "../record/l1-writer.js";
 import type { EmbeddingProviderInfo } from "./embedding.js";
 import type {
@@ -113,6 +112,22 @@ interface Logger {
   error: (message: string) => void;
 }

+type StatementSync = {
+  run(...params: unknown[]): unknown;
+  get(...params: unknown[]): unknown;
+  all(...params: unknown[]): unknown;
+};
+
+type DatabaseSync = {
+  exec(sql: string): unknown;
+  prepare(sql: string): StatementSync;
+  close(): void;
+  loadExtension(path: string): void;
+  enableLoadExtension?(allow: boolean): void;
+};
+
+type DatabaseSyncCtor = new (path: string, options?: unknown) => DatabaseSync;
+
 const TAG = "[memory-tdai][sqlite]";

 /** Persisted metadata about the embedding provider used to generate stored vectors. */
@@ -134,10 +149,16 @@ export interface VectorStoreInitResult {
   reason?: string;
 }

-// Use createRequire to load the experimental node:sqlite module
 const require = createRequire(import.meta.url);

-function requireNodeSqlite(): typeof import("node:sqlite") {
+function requireCompatibleSqlite(): { DatabaseSync: DatabaseSyncCtor } {
+  if (process.versions.bun) {
+    const { Database } = require("bun:sqlite") as typeof import("bun:sqlite");
+    return {
+      DatabaseSync: Database as unknown as DatabaseSyncCtor,
+    };
+  }
+
   return require("node:sqlite") as typeof import("node:sqlite");
 }

@@ -415,8 +436,8 @@ export class VectorStore implements IMemoryStore {
     this.logger = logger;

     // Open database with extension support enabled
-    const { DatabaseSync: DbSync } = requireNodeSqlite();
-    this.db = new DbSync(dbPath, { allowExtension: true });
+    const { DatabaseSync: DbSync } = requireCompatibleSqlite();
+    this.db = new DbSync(dbPath, process.versions.bun ? undefined : { allowExtension: true });

     // Set busy timeout so concurrent processes retry instead of failing with SQLITE_BUSY
     this.db.exec("PRAGMA busy_timeout = 5000");
@@ -458,7 +479,7 @@ export class VectorStore implements IMemoryStore {
     try {
       // eslint-disable-next-line @typescript-eslint/no-require-imports
       const sqliteVec = require("sqlite-vec");
-      this.db.enableLoadExtension(true);
+      this.db.enableLoadExtension?.(true);
       sqliteVec.load(this.db);
     } catch (err) {
       const message = err instanceof Error ? err.message : String(err);
